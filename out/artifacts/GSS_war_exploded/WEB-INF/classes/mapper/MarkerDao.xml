<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="pers.hzh.gss.dao.MarkerDao">
    <resultMap id="ResultMap" type="pers.hzh.gss.model.Marker" >
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="title" property="title" jdbcType="VARCHAR" />
        <result column="note" property="note" jdbcType="VARCHAR" />
        <result column="point" property="point" jdbcType="VARCHAR" />
        <result column="type" property="type" jdbcType="VARCHAR" />
    </resultMap>
    <select id="selectNoteByTitle" resultMap="ResultMap">
        select
        note,level
        from
        marker
        where
        title=#{title}
    </select>
    <select id="selectAllMarker" resultMap="ResultMap">
        select
        id,title,note,point,type
        from
        marker
    </select>
    <insert id="insertMarker">
        insert into marker
        (title,note,point,type,level)
        values
        (#{title},#{note},#{point},#{type},#{level})
    </insert>
    <delete id="deleteMarkerByTitle">
        delete from marker
        where
        title=#{title}
    </delete>
    <update id="updataNoteByTitle">
        update marker
        set
        note=#{note},level=#{level}
        where
        title=#{title}
    </update>
</mapper>